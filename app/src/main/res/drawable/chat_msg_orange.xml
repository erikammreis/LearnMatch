<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners
        android:bottomRightRadius="8dp"
        android:topRightRadius="8dp" />
    <solid android:color="@color/orangeLight" /> <!-- Cor de fundo -->
    <stroke
        android:width="2dp"
        android:color="@color/orangeLight" /> <!-- Cor da borda laranja -->
</shape>
